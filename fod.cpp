#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);

    vector<int> fod(5);

    for(int i = 0; i < 5; i++){
        cin >> fod[i];
    }


    cout << "FOD FOD FOD" << flush;

    for(int i = 0; i < 5; i++){
        cout << fod[i] << " FODSPACE " << flush;
    }

}