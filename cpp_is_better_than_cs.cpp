#include <bits/stdc++.h>
#define ll long long
using namespace std

int main(){
    cin.tie(0)->sync_with_stdio(0);

    cin >> vector<int> vect;

    for(int i = 0; i < vect.length(); i++){
        cout << vect[i] << " " << flush;
    }
}